{% extends 'base.html.twig' %}

{% block title %}All programs{% endblock %}

{% block body %}
{#    {{ form_start(form) }}
    {{ form_row(form.searchField) }}
    <button type="submit" name="btnSearch">Rechercher</button>
    {{ form_end(form) }}#}




    {{ form_start(form) }}
    {{ form_widget(form.name) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
    {{ form_end(form) }}




    {% for program in programs %}
        <div>
            <img class=" align-self-start ml-5 style:" height=200px" src="{{program.poster}}" alt="{{ program.title }} poster">
            <div>
                <h1 class="ml-3 mt-0">{{ program.title }}</h1>
                <p class=" ml-5 style:" whith=50%">{{ program.summary }}</p>
                <p class =" ml-5">Categories : {{ program.category.name }}</p>
            </div>

        </div>

    {% endfor %}


{% endblock %}
